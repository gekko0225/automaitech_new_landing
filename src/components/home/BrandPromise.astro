<section class="brand-promise">
  <div class="container">
    <div class="heading">
      <span class="eyebrow">Qué podemos implementar</span>
      <h2>La tecnología se adapta al proceso. No al revés.</h2>
      <p>Según el cuello de botella, combinamos las piezas necesarias para que el sistema funcione de principio a fin.</p>
    </div>

    <div class="cards">
      <article class="card"><h3>Captura y calificación</h3><p>Landing pages, formularios, WhatsApp y reglas que ayudan a registrar y priorizar prospectos.</p></article>
      <article class="card"><h3>Seguimiento y agenda</h3><p>Flujos automáticos, recordatorios y conexión con calendarios para evitar oportunidades olvidadas.</p></article>
      <article class="card"><h3>Agentes de IA</h3><p>Cuando aportan valor, incorporamos agentes para responder, orientar, calificar o apoyar el seguimiento.</p></article>
    </div>
  </div>
</section>

<style>
  .brand-promise { padding: 120px 0; background: #f8fafc; }
  .heading { max-width: 760px; margin: auto; text-align: center; }
  .eyebrow { display: inline-block; margin-bottom: 18px; color: #2563eb; font-size: 13px; font-weight: 800; letter-spacing: .14em; text-transform: uppercase; }
  .heading h2 { margin: 0; color: #0f172a; font-size: clamp(2.4rem, 5vw, 4rem); line-height: 1.05; font-weight: 900; letter-spacing: -.05em; }
  .heading p { max-width: 650px; margin: 28px auto 0; color: #475569; font-size: 18px; line-height: 1.8; }
  .cards { display: grid; gap: 24px; margin-top: 70px; }
  .card { border: 1px solid #e2e8f0; border-radius: 24px; background: white; padding: 34px; box-shadow: 0 18px 45px rgba(15,23,42,.05); transition: .25s ease; }
  .card:hover { transform: translateY(-6px); border-color: #bfdbfe; box-shadow: 0 25px 55px rgba(15,23,42,.08); }
  .card h3 { margin: 0; color: #0f172a; font-size: 26px; font-weight: 800; letter-spacing: -.03em; }
  .card p { margin-top: 16px; color: #475569; line-height: 1.8; }
  @media (min-width: 900px) { .cards { grid-template-columns: repeat(3, 1fr); } }
  @media (max-width: 768px) { .brand-promise { padding: 90px 0; } }
</style>
